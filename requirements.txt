# Core (required)
# NOTE: previously pinned to exact versions (==) for reproducibility, but
# that backfired hard on Streamlit Cloud: their platform is currently
# forcing Python 3.14 regardless of runtime.txt (a confirmed, open
# platform bug as of mid-2026 -- see README §0.1), and several of the old
# exact pins here predate Python 3.14's existence, so no matching wheel
# exists for them, forcing slow/broken from-source builds during deploy.
# Switched to minimum-version floors (>=) instead, so pip/uv resolves to
# whatever current release actually has a 3.14 wheel. Every floor below
# has been directly verified working against this project's own test
# suite on newer versions (numpy 2.4.4, pandas 3.0.2, scipy 1.17.1,
# Pillow 12.1.1, opencv 4.13.0) -- not a guess, actually run.
numpy>=1.26
pandas>=2.2
opencv-python-headless>=4.9
streamlit>=1.50
matplotlib>=3.8
requests>=2.31
Pillow>=10.3

# 3D web dashboard (server.py) — serves the live simulation over HTTP to
# the Three.js front-end in policysim/static/
fastapi>=0.110
uvicorn>=0.29

# Statistical experiments (experiments/)
scipy>=1.13

# Optional: only needed if you want to print physical QR trigger cards
qrcode[pil]>=7.4

# Optional: only needed for the "groq" reasoning backend (hosted, needs internet)
# no extra package beyond `requests`, already listed above

# Dev/testing only: needed to run the regression suite in tests/
pytest>=7.4

# Report generation (build_report.py) — optional, only needed to regenerate
# the PDF results report
reportlab>=4.4

# Note on the "ollama" reasoning backend:
# Ollama itself is not a pip package. Install it separately from https://ollama.com,
# then run:  ollama pull llama3.1:8b   and   ollama serve
# PolicySim talks to it over HTTP (http://localhost:11434), so no python
# client library is required.
