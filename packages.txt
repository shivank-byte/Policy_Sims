# Streamlit Cloud reads this file and apt-get installs these before pip
# install -- without them, `import cv2` from the full (GUI-enabled)
# opencv-python package fails with:
#   ImportError: libGL.so.1: cannot open shared object file: No such file or directory
# because Streamlit Cloud's base container doesn't ship the GUI libraries
# opencv-python links against, even though this app never actually opens a
# GUI window (see streamlit_app.py's camera trigger, which only uses
# cv2's pure array/image-processing functions).
libgl1
libglib2.0-0
